---
layout: default
---

<div class="post">
  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
    {% if page.author %}
      <p class="post-meta">{{ page.author }}</p>
    {% endif %}
  </header>

  <article>
    {{ content }}
  </article>
</div>
